apiVersion: apps/v1
kind: Deployment
metadata:
  name: PLACEHOLDER_NAME
  namespace: core-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: PLACEHOLDER_NAME
  template:
    metadata:
      labels:
        app: PLACEHOLDER_NAME
    spec:
      containers:
      - name: frr
        image: quay.io/frrouting/frr:9.1.1
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - name: frr-cfg
          mountPath: /etc/frr/frr.conf
          subPath: frr.conf
        - name: frr-cfg
          mountPath: /etc/frr/vtysh.conf
          subPath: vtysh.conf
      volumes:
      - name: frr-cfg
        configMap:
          name: frr-cfg
---
apiVersion: v1
kind: Service
metadata:
  name: PLACEHOLDER_NAME-svc
  namespace: core-router
spec:
  selector:
    app: PLACEHOLDER_NAME
  ports:
  - name: vty
    port: 2601
    targetPort: 2601